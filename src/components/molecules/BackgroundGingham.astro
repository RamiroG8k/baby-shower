---
/**
 * BackgroundGingham Component
 * Generates the checkered pattern seen in IMG_5077.jpeg
 */
---

<div class="gingham-pattern">
    <div class="white-background"></div>

    <slot />
</div>

<style>
    .gingham-pattern {
        height: 100%;
        width: 100%;
        aspect-ratio: 5 / 7;
        overflow: hidden;
        position: fixed;
        inset: 0;
        z-index: -1;
        padding: 2rem;
        opacity: 50%;

        background-color: var(--gingham-bg);
        background-image:
            linear-gradient(
                90deg,
                rgba(var(--gingham-stripe), 0.5) 50%,
                transparent 50%
            ),
            linear-gradient(rgba(var(--gingham-stripe), 0.5) 50%, transparent 50%);
        background-size: 60px 60px; /* Size of the checkered squares */
    }

    .white-background {
        display: none;
        position: absolute;
        inset: 0;
        background-color: var(--gingham-content-bg);
        width: 90%;
        max-width: 900px;
        margin: 0 auto;
    }

    @media screen and (min-width: 1200px) {
        .gingham-pattern {
            opacity: 100%;
        }

        .white-background {
            display: block;
        }
    }
</style>
